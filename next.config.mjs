/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['mp.weixin.qq.com', 'www.facebook.com', 'wordpress.com', 'twitter.com', 'magento.com', 'en.wikipedia.org', 'business.linkedin.com']
    }
};

export default nextConfig;
